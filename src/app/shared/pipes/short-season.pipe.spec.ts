import { ShortSeasonPipe } from './short-season.pipe';

describe('ShortSeasonPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortSeasonPipe();
    expect(pipe).toBeTruthy();
  });
});
